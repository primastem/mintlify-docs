---
title: "User Manual"
description: '**PrimaSTEM** — a complex for teaching children from age 4 the basics of logic, programming, and mathematics.'
icon: 'book-open'
---

## Technical Specifications and Package Contents

![primastem](../images/hello.png)

- Game robot
- Robot control panel
- Command, value, and arithmetic operation tokens for creating programs

> Package contents and appearance may vary slightly. Clarify at the time of purchase.

### Game Robot

Dimensions: D=125 mm, H=44 mm.

![robot](../images/robot.png)

The robot is equipped with a power button, LEDs, a speaker, a function button, and a USB-C port for charging the battery.

A marker with a diameter of up to 10 mm can be installed in the center of the robot for drawing simple shapes while moving.

Appearance may vary slightly depending on the package, but the main functionality is preserved.

### Control Panel

Dimensions: L=317 mm, W=217 mm, H=62 mm.

![control](../images/control.png)

The control panel contains 11 paired cells for installing command and value tokens: 6 — for the main program (upper part), 5 — for the subroutine (lower part).
The panel has two buttons: left — power, right — "Execute / Stop" for starting and stopping the program.

After installing command and value tokens in the cells, press "Execute": the robot will execute the program. Active commands are highlighted by LEDs between the cells.

If a token is installed incorrectly — the panel signals with a red LED, but the program continues (for example, when installing a value token without a command).

The panel is equipped with a USB-C port for charging and a speaker for sound reproduction.

### Instruction Tokens

Dimensions: LxW=33 mm.

The set includes tokens for creating a program.
Each token is a command with a clear value and instruction. The sequence of blocks determines the robot's behavior.

![coding chips](../images/tokens.png)

Tokens are divided into **commands**, **values**, and **arithmetic**.

#### Command Tokens

Main blocks for creating a control program:

- **Forward** — forward movement (15 cm by default)
- **Right** — turn 90° clockwise
- **Left** — turn 90° counterclockwise
- **Back** — backward movement (15 cm by default)
- **Function** — execute subroutine from the lower part of the panel
- **Random Movement** — one of the actions for moving the robot: Forward, Left, Right, or Back (by "default step")
- **Repeat (loop) Tokens** - Tokens with numbers from 2 to 6 (by number of dots) and repeat pictograms with a number inside. A token with a die means a random number of repeats from 1 to 6.

> Value and arithmetic tokens are also used to expand programming: for example, repeating a command or changing the angle/distance of movement.

#### Value Tokens

Angle and distance tokens: 30°, 36°, 45°, as well as multiples (60°, 72°, etc.).
Angles in degrees, distances — in millimeters.
By default — 90° and 150 mm (15 cm).

#### Arithmetic Operation Tokens

Change movement command parameters:

- Addition (+)
- Subtraction (−)
- Multiplication (*)
- Division (/)
- Square root (√)
- Exponentiation (^)

## Connecting the Panel and Robot

It is recommended to turn on the robot first, then the panel.

Place the robot on a flat surface before connecting: after connection, the LEDs turn white. If there is no connection — they blink red.

Check the connection: install the "Forward" token and press "Execute".

If there is no connection — restart both devices or charge them. Temporary loss of connection is possible near sources of strong electromagnetic fields (mobile phone, WiFi point).

> **After updating device software** pairing may be required: turn on the robot, then the panel, press and hold the "Execute" button on the panel for 10–15 seconds, until a sound signal appears.

## How Does It Work?

To program movement, install command tokens in the panel cells (for example, "Forward", "Left", "Function", etc.).
You can install command values and number of repeats, as well as angle, distance, and arithmetic operation values.

For joint installation of a command and value (repeat), cells are combined with "bridges" with indicators.

  *Below is a program using a loop as an example to repeat a similar code section. The program creates a route for the robot to move in a square:*

![control with chips](../images/manual1a.excalidraw.svg)

Commands are installed first in the upper part of the panel (6 cells) and executed from left to right. Empty cells or errors are ignored (for example, two commands in one block or a value without a command).

The sequence of blocks determines the robot's movement.

Press "Execute" to start the program.

By default:
- "Forward" — 15 cm.
- "Left/Right" — 90°.
- Loops (repeats) repeat the command several times.
- "Function" calls a subroutine from the lower part of the panel (5 cells).
- You can call a subroutine multiple times with a loop by adding a repeat token to "Function" (example shown above).

### Important Functions:

- **Interrupt** program execution by pressing "Execute" again during movement.
- The panel remembers the last **set value** (distance/angle) for a movement command until power off: for example, if "Forward" = "200", all subsequent "Forward" commands are executed with this value.
- Values after arithmetic are also saved.
- You can change the "**default distance**" with a service token: for example, for 12.5 cm, install the "Default Distance" token with value 125. The value is saved after power off.
- The panel and robot **automatically turn off** after 10 minutes of inactivity.
- If the robot is not used for 1–3 minutes, it makes small movements, signaling operation and readiness.
- **Robot eye blink sound** can be turned off and on with a single button press on the robot.
- **Control panel sound** can be turned off and on by holding the "Execute" button for 5 seconds.
- **After software update** robot movement calibration may be required.
- **Robot software reset** is possible by holding the button for 10 seconds.
- **Panel and robot pairing** may be necessary after updating device software, established by holding the "Execute" button on the panel for 10-15 seconds.

### Robot Calibration:

- Mark the exact position of the robot's direction on the table surface.
- Using any program, you need to achieve a robot turn 8 times at the default angle - 90 degrees clockwise (for example, execute a right turn 8 times), the robot must make two full revolutions of 360 degrees = 90*8.
- Then you need to evaluate the deviation angle from the initial robot position and execute the "Calibration" command with the value necessary for normalization: if the robot stopped before the initial mark, then add the arithmetic operation "+ angle" (for example +10), if the robot stopped after the mark, then add "- angle" (for example -5).
- Calibration can be performed several times to achieve the best result.
- Attention! Robot calibration reset is performed by pressing the control button for more than 10 seconds.

<Accordion icon="rectangle-terminal" title="Calibration of the first version of the device (MVP) with a 10 cm diameter robot">
1. To calibrate the gyroscope, place the robot on a horizontal surface, insert the "Calibration" token into the panel and press "Execute".
2. To calibrate the movement distance, measure the actual path by the "Forward" command, install "Calibration" and a token with the required value, then "Execute". You can write the value to any digital NFC token using a mobile phone with NFC as text in the form nXXX (for example, n095 for 95 mm). After this, the robot's movement length will be accurate.*
</Accordion>

### Programming NFC Tokens

You can change or **create your own tokens** — commands, digital values, or arithmetic operations using a phone with NFC and a tag reading/writing program, such as "NFC Tools". You can write a value to a blank digital NFC token using a mobile phone with NFC as simple text in the form of 4 characters.

## Important!

> The set **is not intended for children under 4 years of age** and contains small parts — **choking hazard**! Use **only under adult supervision**!

The device contains Li-ion batteries. **Charge only under adult supervision** using standard USB 5V and USB-C cable.

Charging the panel and robot (1 hour) is enough for 1–2 lessons; a full charge (2–3 hours) — for 3 lessons of 30–45 minutes. Over time, battery capacity may decrease, replacement with new 16340 Li-ion batteries is possible.

If the set is not used for a long time, charge the batteries **once every 2-3 months**. With deep discharge, batteries may fail and require replacement.

**Do not open the device** yourself — in case of breakdown or the need to replace batteries, contact the seller or special electronics repair places.

Store and use the set in a dry room at a temperature of +10…+30°C, humidity 45–60%. Avoid direct sunlight, moisture, dust.

Avoid shocks and vibrations. Transport and store the device in the original packaging at a temperature of +5…+35°C, protecting from mechanical damage and moisture.

p.s.: PrimaSTEM is a practical tool for learning programming, developing creative thinking and logic.
Detailed information about functionality is contained in the **Teacher's Guide**.
